# pow

`pow` (project oriented workspace) is a CLI that manages multi-repo workspaces via git worktrees. Group git worktrees from one or more source repos into a single directory, all checked out on a shared branch named after the workspace, and run your tools scoped to exactly the repos you care about.

Requires `git` on PATH. See `pow --help` for usage.
